// Collision.cpp

// Includes
#include "CollisionData.h"


CollisionData::CollisionData(CollisionFlagTag defaultFlag) : mFlags(defaultFlag),
                                                             mDX(0),
                                                             mDY(0) {
    // empty
}
